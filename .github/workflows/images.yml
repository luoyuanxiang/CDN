name: "🛠 压缩图片"
on:
  push:
    branches:
      - main
jobs:
  build:
    name: calibreapp/image-actions
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v3